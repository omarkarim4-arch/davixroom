# Design reference

Inspiration, screenshots, mood boards, and spec images for the DavixRoom
design phase. Nothing here is served to users — it exists to inform the UI.

Ship-ready brand assets belong in `public/brand/` instead.
